name: xinzuo_app
description: 心作 - 网文创作工具移动端
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  go_router: ^13.0.1
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  flutter_slidable: ^3.0.1
  shimmer: ^3.0.0
  flutter_animate: ^4.3.0
  iconsax_flutter: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
